/* Escreva um programa que leia o tamanho do lado de um quadrado e imprima um quadrado
daquele tamanho com asteriscos.
 O programa deve funcionar para quadrados com lados de todos os tamanhos entre 2 e 20.*/

#include <stdio.h>

int main() {
    
    int indiceum, indicedois, soma, i, j;
    int matriz[4][4] = {0, 10, 8, 20,
                        10, 0, 25, 30,
                        0, 25,  0, 12,
                        20, 30, 12, 0};
                        
                        
    
    for(i=0; i<4; i++){
        for(j=0; j<4; j++){
            printf("%d  ", matriz[i][j]);
        }
        printf("\n");
    }
    
    
    printf("\nDigite o primeiro indíce: ");
    scanf("%d", &indiceum);
    
    indiceum = matriz[]
    
    printf("Digite o segundo indíce: ");
    scanf("\n%d", &indicedois);
    
    
    

    return 0;
}
